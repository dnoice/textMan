# ============================================================================
# textMan Environment Configuration
# ============================================================================
#
# IMPORTANT: Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it's already in .gitignore
#
# Usage:
#   1. Copy: cp .env.example .env
#   2. Fill in your Firebase credentials
#   3. Update feature flags as needed
#
# ============================================================================

# ============================================================================
# Firebase Configuration (REQUIRED for upcoming sessions)
# ============================================================================
# Get these values from: https://console.firebase.google.com
# Project Settings > General > Your apps > Web app > Config

# Firebase API Key
# This is safe to expose in client-side code (protected by security rules)
VITE_FIREBASE_API_KEY=your-api-key-here

# Firebase Auth Domain
# Format: your-project-id.firebaseapp.com
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase Project ID
# Your unique Firebase project identifier
VITE_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket
# Format: your-project-id.appspot.com
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase Messaging Sender ID
# Used for Firebase Cloud Messaging
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012

# Firebase App ID
# Your Firebase web app identifier
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# Firebase Measurement ID (optional - for Analytics)
# Format: G-XXXXXXXXXX
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================================
# Application Configuration
# ============================================================================

# Environment (development, staging, production)
NODE_ENV=development

# Debug Mode (shows console logs, performance metrics)
# Set to false in production
DEBUG_MODE=true

# Application Version (should match package.json)
VITE_APP_VERSION=2.1.0

# ============================================================================
# Feature Flags
# ============================================================================

# Enable Firebase Integration
VITE_ENABLE_FIREBASE=false

# Enable user authentication (requires Firebase)
VITE_ENABLE_AUTH=false

# Enable cloud sync (save texts to Firebase)
VITE_ENABLE_CLOUD_SYNC=false

# Enable collaborative editing (future feature)
VITE_ENABLE_COLLABORATION=false

# Enable telemetry/analytics
VITE_ENABLE_ANALYTICS=false

# Enable experimental features
VITE_ENABLE_EXPERIMENTAL=false

# ============================================================================
# External Resources
# ============================================================================

# Font Awesome CDN URL
# Can be overridden to use local copy or different CDN
VITE_FONT_AWESOME_CDN=https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css

# ============================================================================
# Performance & Limits
# ============================================================================

# Maximum file size for import (in bytes)
# Default: 10MB (10 * 1024 * 1024)
# Recommended: 5MB for better performance
VITE_MAX_FILE_SIZE=5242880

# Maximum saved texts
VITE_MAX_SAVED_TEXTS=100

# Maximum history items
VITE_MAX_HISTORY=50

# Auto-save delay (milliseconds)
VITE_AUTO_SAVE_DELAY=2000

# Toast notification duration (milliseconds)
VITE_TOAST_DURATION=3000

# ============================================================================
# Security Configuration
# ============================================================================

# Content Security Policy mode (report-only, enforce)
VITE_CSP_MODE=report-only

# Enable HTTPS-only mode (production should be true)
VITE_FORCE_HTTPS=false

# Session timeout (minutes) - for authenticated users
VITE_SESSION_TIMEOUT=60

# ============================================================================
# Development Tools
# ============================================================================

# Enable source maps in production (set to false for security)
VITE_ENABLE_SOURCE_MAPS=true

# Enable hot module replacement (development only)
VITE_HMR=true

# Development server port
VITE_DEV_PORT=5173

# ============================================================================
# Firebase Emulator (for local development)
# ============================================================================

# Use Firebase emulator instead of production
VITE_USE_FIREBASE_EMULATOR=false

# Emulator host
VITE_FIREBASE_EMULATOR_HOST=localhost

# Firestore emulator port
VITE_FIRESTORE_EMULATOR_PORT=8080

# Auth emulator port
VITE_AUTH_EMULATOR_PORT=9099

# Storage emulator port
VITE_STORAGE_EMULATOR_PORT=9199

# ============================================================================
# Notes
# ============================================================================
#
# VITE_ prefix: Required for Vite to expose variables to client-side code
# Environment priority: .env.local > .env.[mode] > .env
# Never commit: .env, .env.local, .env.*.local (already in .gitignore)
#
# For production deployment:
#   1. Set all sensitive values via hosting platform (Vercel, Netlify, etc.)
#   2. Set NODE_ENV=production
#   3. Set DEBUG_MODE=false
#   4. Set VITE_FORCE_HTTPS=true
#   5. Set VITE_ENABLE_SOURCE_MAPS=false
#
# ============================================================================
