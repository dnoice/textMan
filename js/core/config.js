/**
 * textMan v2 - Application Configuration
 * Core configuration constants
 */

const APP_CONFIG = {
    name: 'textMan',
    version: '2.1a.0',
    maxHistory: 50,
    maxSaved: 100,
    maxFileSize: 10 * 1024 * 1024, // 10MB
    autoSaveDelay: 2000,
    toastDuration: 3000,
    debounceDelay: 300
};
